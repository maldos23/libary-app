# ── Desarrollo local ──────────────────────────────────────────────
# Deja este valor en /api: Vite hace proxy de /api -> backend local
# (no se produce CORS porque la peticion sale del propio servidor de Vite)
VITE_API_BASE_URL=/api

# Opcional: cambia el target del proxy si tu backend corre en otro puerto
# VITE_PROXY_TARGET=http://localhost:8080

# ── Produccion (Vercel) ────────────────────────────────────────
# Configura en Vercel: Settings -> Environment Variables
#   VITE_API_BASE_URL = https://<tu-servicio>.a.run.app/api
